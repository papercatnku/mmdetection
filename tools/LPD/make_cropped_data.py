import numpy as np
import os


def ran_croproi_if_available(
        src_img_fn,
        min_w_ratio=0.1,
        min_h_ratio=0.1,
        

)

def process_dir(
        src_dir,
        dst_dir,
        expand_w_ratio=8,
        expand_h_ratio=18):

    os.makedirs(dst_dir, exist_ok=True)

    return
